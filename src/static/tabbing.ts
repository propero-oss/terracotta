/**
 * Tab a given amount of steps.
 * @param {number} [steps=1] The number of steps to tab. May be negative.
 */
export function tab(steps: number=1): Element {
  return;
}

/**
 * Tab to the given element or its first tabbable child.
 * @param {string|Element} element The element to tab to.
 */
export function tabTo(element: string | Element): Element {
  return;
}

/**
 * Tab into the first tabbable child of the given element.
 * @param {string|Element} element The element to tab into.
 */
export function tabInto(element: string | Element): Element {
  return;
}

/**
 * Tab a given amount of steps starting at the given element.
 * Does never enter the given element again.
 * @param {string|Element} element The element to tab out of.
 * @param {number} [steps=1] The number of steps to tab. May be negative.
 */
export function tabOutOf(element: string | Element, steps: number=1): Element {
  return;
}
